# Never commit the real .env — it is gitignored.
OPERATOR_PRIVATE_KEY=0x...        # signs the batch, the approval, and the x402 fee
BASE_RPC_URL=https://...          # Base mainnet (chain 8453)
SUPABASE_SERVICE_ROLE_KEY=...     # the operator's own self-hosted VMflow stack
PAYOUT_RECIPIENT_2=0x...          # recipient A of the two-way split
